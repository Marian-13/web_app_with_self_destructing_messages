source 'https://rubygems.org'

# Sinatra is a DSL for quickly creating web applications in Ruby with minimal effort
gem 'sinatra', '~>1.4.7'

# Mapping database tables to Ruby classes
gem 'data_mapper'
# PostgreSQL Adapter for DataMapper
gem 'dm-postgres-adapter'

# Ruby interface to the {PostgreSQL RDBMS}[http://www.postgresql.org/]
gem 'pg', '~>0.19.0'

# Rake is a Make-like program implemented in Ruby
gem 'rake', '~>11.3.0' # to apply migrations

# The bcrypt provides a simple wrapper for safely handling passwords.
gem 'bcrypt', '~>3.1.11'

# Simple flash hash implementation for Rack apps
gem 'rack-flash3', '~>1.0.5'

group :development do
  # Restarts your app when a file changes
  gem 'rerun', '~>0.11.0'
end
